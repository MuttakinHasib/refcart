import NavBar from './NavBar';
import TopBar from './TopBar';

const Header = () => {
  return (
    <header>
      <>
        <TopBar />
        <NavBar />
      </>
    </header>
  );
};

export default Header;
